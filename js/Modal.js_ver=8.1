
// Get the modal
var modal_sell = document.getElementById('priceForm');
var modal_transfer = document.getElementById('transferForm');
var modal_mm = document.getElementById('loginMetamask');
var modal_share = document.getElementById('shareLambo');
// Get the button that opens the modal
var btn = [];
var btn_buy = [];
// Get the <span> element that closes the modal
var span_sell = document.getElementsByClassName("close_sell")[0];
var span_transfer = document.getElementsByClassName("close_transfer")[0];
var span_mm = document.getElementsByClassName("close_mm")[0];
var span_share = document.getElementsByClassName("close_share")[0];
// When the user clicks the button, open the modal 


// When the user clicks on <span> (x), close the modal
span_sell.onclick = function() {
    modal_sell.style.display = "none";
}
span_transfer.onclick = function() {
    modal_transfer.style.display = "none";
}
span_mm.onclick = function() {
    modal_mm.style.display = "none";
}
span_share.onclick = function() {
    modal_share.style.display = "none";
}
// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
    if (event.target == modal_sell) {;
        modal_sell.style.display = "none";
	}
    if (event.target == modal_transfer) {
        modal_transfer.style.display = "none";
	}
    if (event.target == modal_mm) {
		modal_mm.style.display = "none";
	}
    if (event.target == modal_share) {
		modal_share.style.display = "none";
	}
}
